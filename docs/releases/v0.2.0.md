# AI Job Skills Lab v0.2.0 — unreleased

This is the next release's prepared scope, not a published release or tag.
The repository remains private. Record the exact committed candidate and successful
CI run in the GitHub release notes when an authorized release is made. Do not move
the existing `v0.1.0` tag (`ba9c04f173ceca15e93fb2edb67b78613253f556`).

## User-visible changes since v0.1.0

- Rename the project to AI Job Skills Lab while preserving upstream attribution,
  legacy private-workspace lookup and interchange withdrawal identity.
- Turn the second report into a connected learning workspace: learning paths,
  fine-grained skills, projects, YouTube experiments and progress. Keep the jobs
  list as its own HTML report, with typeahead and useful location/role filters.
- Add exact skill evidence, explicit denominators, descriptive 30-day windows,
  bounded resumable analysis, path selection and reviewed progress/profile updates.
- Corrected progress now retracts dependent briefs and managed reports as well
  as profile claims. Existing stores reconcile on their next supported operation;
  managed recovery also applies the correction lifecycle before restoring reports.
- Add an account-free synthetic demo, a verified pinned-runtime setup recipe,
  complete bundled-font notices and a local release guard matching CI's version check.

## Compatibility and costs

No dependency, model, database or schema migration is introduced by the readiness
fixes. Preserve the current withdrawal journal and use the supported writer;
do not downgrade to recover invalidated drafts. Regenerate affected briefs and
reports after corrections. Independently invalid result contexts require explicit
withdrawal, and browser copies cannot be recalled.

Model execution remains pinned to the inspected Codex 0.153.4 macOS arm64 binary
and gpt-5.5. No new platform or model is qualified. Job data has a ₹0 services
budget; hosted processing requires compatible permissions and existing ChatGPT
access. The offline demo needs only Python 3.10+ and no authentication.

Original project additions remain MIT. The identified Codex adaptation remains
Apache-2.0, and bundled Lato/Raleway fonts remain OFL-1.1. Preserve all notices.

## Validation and remaining gates

See [the readiness-fix validation record](../research/release-readiness.md).
Fixture results establish deterministic behavior, not real-world recommendation
quality, audience demand, commercial potential or successful learning outcomes.
Human quality targets, source activation and unattended inference remain separate.

Before tagging: review the exact candidate diff, run all required checks, confirm
GitHub CI for that SHA, and resolve the maintainer-approved private reporting
route in SECURITY.md. A public visibility change needs separate owner authorization.
